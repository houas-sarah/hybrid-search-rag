# --- API layer ---
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# --- Configuration ---
pydantic>=2.7.0
pydantic-settings>=2.3.0

# --- Dense retrieval (vector DB + local embeddings/reranker) ---
chromadb>=0.5.0
sentence-transformers>=3.0.0

# --- Sparse retrieval ---
rank-bm25>=0.2.2

# --- Generation backends (both optional at runtime) ---
ollama>=0.3.0        # default local backend
groq>=0.9.0          # optional free-tier cloud backend

# --- Utilities ---
numpy>=1.26.0
